
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm   box-shadow mb-3" >
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between" style="margin-top:-60px;">
                    <ul class="navbar-nav flex-grow-1">

                    </ul>
                        <div class="ml-auto mx-5 " >

                    @if (User.Identity.IsAuthenticated)
                    {
                                <form id="logoutForm" asp-controller="Login" asp-action="Logout" method="post" class="form-inline">
                            @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-danger">Log Out</button>
                                </form>
                    }
                    else
                    {
                                    <a type="submit" class="btn btn-outline-dark" href="/Register/Index">Sign Up</a>

                                <a type="submit" class="btn btn-outline-dark" href="/Login/Index">Sign In</a>
                    }
                </div>
                </div>
            </div>
        </nav>
    </header>